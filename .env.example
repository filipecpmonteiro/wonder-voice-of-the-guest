# Anthropic API — only needed to re-run `npm run analyze` (regenerate strategic-all.json).
# NOT required to serve the site.
ANTHROPIC_API_KEY=

# SQLite location. Defaults to ./data/wonder.db. On Railway, set to /data/wonder.db and mount a volume.
DB_PATH=
